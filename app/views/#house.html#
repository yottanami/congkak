<div
  class="{{numbers[id_number]}} house"
  ng-click="trigleClick(id_number, state)" ng-init="i=0">
  <img
  class="ball {{numbers[$index+1]}}" src="/images/ball.png"
  ng-repeat="t in getTimes(state) track by $index"
  ng-animate="'animate'" 
  >
  <br>
  <div>{{state}}</div>
</div>
